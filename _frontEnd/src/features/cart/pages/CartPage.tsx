/**
 * CartPage.tsx — Página pública del Carrito de Compras.
 * Carga lazy vía router.tsx → /carrito
 */

import CartView from '../components/CartView'

export default function CartPage() {
  return <CartView />
}
